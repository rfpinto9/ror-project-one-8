<%= form_with(model: director, local: true) do |form| %>
  <% if director.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(director.errors.count, "error") %> prohibited this director from being saved:</h2>

      <ul>
      <% director.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, :class => 'form-control myform' %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age, :class => 'form-control myform' %>
  </div>

  <div class="field">
    <%= form.label :oscars %>
    <%= form.number_field :oscars, :class => 'form-control myform' %>
  </div>

  <div class="actions">
    <%= form.submit({:class => 'btn btn-success'}) %>
  </div>
<% end %>
